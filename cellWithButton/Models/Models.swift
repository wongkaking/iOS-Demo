import Foundation

final class CheckBoxModel {
    let text: String
    var isChecked: Bool

    init(text: String, isChecked: Bool) {
        self.text = text
        self.isChecked = isChecked
    }
}
